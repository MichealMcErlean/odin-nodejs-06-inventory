<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('partials/header.ejs') %>
  <%- include('partials/nav') %>
  <article>
    <div class="gameList">
      <% if (games.length === 0) {%>
        <p>No entries in database.</p>
      <% } else {%>
        <% games.forEach(game => { %>
          <span><a href="<%= `/games/${game.game_id}` %>"><%= game.name %></a></span>
        <% }); %>
      <% } %>
    </div>
  </article>
  <%- include('partials/footer.ejs') %>
</body>
</html>
