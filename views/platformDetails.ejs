<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('partials/header.ejs') %>
  <%- include('partials/nav') %>
  <article>
    <% if (platform.length===0) {%>
      <p>No platform has that ID.</p>
      <% } else { %>
        <h1>
          <%= platform[0].platform %>
        </h1>
        <div class="buttonBox">
          <a href="/platforms/update/<%= platform[0].platform_id %>">Update Details</a>
          <form action="/platforms/delete/<%= platform[0].platform_id %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this platform?');">
            <button type="submit" class="fakelink">Delete Platform</button>
          </form>
        </div>
        <h2>Games on the <%= platform[0].platform %>
        </h2>
        <% if (games.length===0) {%>
          <p>No games on record.</p>
          <% } else { %>
            <div class="gameList">
              <% games.forEach(game=> { %>
                <div class="gameLink">
                  <a href="/games/<%= game.game_id %>">
                    <%= game.name %>
                  </a>
                </div>
              <% }); %>
            </div>
          <% } %>
        <% } %>
  </article>
  <%- include('partials/footer.ejs') %>
</body>
</html>