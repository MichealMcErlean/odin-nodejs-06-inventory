<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('partials/header.ejs') %>
  <%- include('partials/nav') %>
  <article class="gameDetails">
    <% if (!game) {%>
      <p>No game has that ID</p>
    <% } else { %>
      <h1><%= game[0].name %></h1>

      <div class="detailBlock">
        <span class="detailLabel">Price: </span>
        <span><%= game[0].price %></span>

        <span class="detailLabel">Quantity: </span>
        <span><%= game[0].quantity %></span>

        <span class="detailLabel">Release Date: </span>
        <span><%= game[0].release_date.toDateString() %></span>
        
        <span class="detailLabel">Developer: </span>
        <span><%= game[0].developer %></span>
        
        <span class="detailLabel">Publisher: </span>
        <span><%= game[0].publisher %></span>

        <span class="detailLabel">Genres: </span>
        <span><%= game[0].genres %></span>

        <span class="detailLabel">Platforms: </span>
        <span><%= game[0].platforms %></span>
      </div>
      
      <div class="buttonBox">
        <!-- <button action="/games/update/<%= game[0].game_id %>" method="GET" type="button">Update Details</button> -->
        <a href="/games/update/<%= game[0].game_id %>">Update Details</a>
        <form action="/games/delete/<%= game[0].game_id %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this game?');">
          <button type="submit" class="fakelink">Delete Game</button>
        </form>
      </div>
    <% } %>
  </article>
  <%- include('partials/footer.ejs') %>
</body>
</html>