<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <%- include('partials/header.ejs') %>
  <%- include('partials/nav') %>
  <article>
    <% if (pub.length === 0) {%>
      <p>No publisher has that ID.</p>
      <% } else { %>
        <h1>
          <%= pub[0].publisher %>
        </h1>
        <div class="buttonBox">
          <a href="/publishers/update/<%= pub[0].publisher_id %>">Update Details</a>
          <form action="/publishers/delete/<%= pub[0].publisher_id %>" method="POST" style="display: inline;" onsubmit="return confirm('Are you sure you want to delete this developer?');">
            <button type="submit" class="fakelink">Delete Developer</button>
          </form>
        </div>
        <h2>Games from <%= pub[0].publisher %>
        </h2>
        <% if (games.length===0) {%>
          <p>No games on record.</p>
          <% } else { %>
            <div class="gameList">
              <% games.forEach(game=> { %>
                <div class="gameLink">
                  <a href="/games/<%= game.game_id %>">
                    <%= game.name %>
                  </a>
                </div>
              <% }); %>
            </div>
        <% } %>
      <% } %>
  </article>
  <%- include('partials/footer.ejs') %>
</body>
</html>